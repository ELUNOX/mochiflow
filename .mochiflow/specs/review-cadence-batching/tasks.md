# Batch bounded in-scope fixes: hold locally, review once before push/accept — Tasks

Implementation Summary: State the in-scope/out-of-scope judgment once in risk.md, split update.md into hold vs. explicit finalize (keeping router.md's PR-feedback summary consistent), generalize open.md's accept-gate freshness trigger, and document build.md's post-completion bounded-fix window; then re-freeze the engine.
risk: elevated
Critical Stop Conditions:
- Do not weaken "a fresh reviewer verdict is required before push/accept" — only the trigger frequency changes, never the invariant.
- Do not touch `build.md`'s task-structure mutation rule, the two delivery approval gates, or `agents/change-reviewer.md`'s diff-scope contract.
- Do not let any edit break an existing pinned conformance-test substring; verify with the full `cargo test` suite before considering a task done.

## Defaults

- Per-task verification: run `mochiflow freeze` first (engine edits change the markdown hashes, so the manifest must be regenerated for `freeze --check` to pass), then `cargo test --manifest-path cli/Cargo.toml && cargo fmt --manifest-path cli/Cargo.toml --all -- --check && cargo clippy --manifest-path cli/Cargo.toml --all-targets -- -D warnings && cargo run --manifest-path cli/Cargo.toml -- freeze --check`. Each engine-editing task includes the freshly frozen `engine/MANIFEST.json` in its own commit so every commit is self-consistent.
- The remaining dogfood steps (`mochiflow upgrade --source engine` to re-vendor `.mochiflow/engine/`, `mochiflow adapter generate --check`) are the final sync in T-005; the vendored copy is gitignored and adapters are byte-identical for prose-only edits, so they are not needed per task.
- Engine edits target repo-root `engine/...` only; never edit the gitignored vendored `.mochiflow/engine/` copy (it is regenerated by `upgrade --source engine`).
- Shared stop conditions: out-of-scope change / new design decision needed / verification keeps failing

## Tasks

- [ ] T-001 [AC-01, AC-03] risk.md: shared judgment + hold/finalize boundary + Reviewed-through convention
  - Depends on: none
  - Files:
    - `engine/reference/risk.md`
    - `engine/templates/spec/design.md`
    - `engine/MANIFEST.json`
    - `cli/crates/mochiflow-cli/tests/conformance.rs`
  - Done: `risk.md`'s "Verdict freshness" paragraph (under `## Consequences`) states, in one place, the judgment separating an in-scope change (no task-structure change, no new AC, no new design decision) from an out-of-scope change, and says `build.md`/`open.md`/`update.md` reference this judgment rather than redefining it. It redefines the freshness boundary as the next push/accept boundary (not every code change): an in-scope change is applied and committed locally (held); the mandatory reviewer re-runs at most once at that boundary, covering every **code-changing** commit held since the last recorded review (a non-code commit such as a `docs(context)` or PR-body-only commit does not by itself make a verdict stale). It adds the `Reviewed through: <sha>` recording convention **on its own line, directly below `Verdict:`** (never combined onto the `Verdict:` line, since `accept.rs::has_passing_review` requires that line to equal exactly `pass`/`pass-with-comments`) in `design.md ## Review Results`, and states the reviewer's diff input is still the full diff from git (frequency changes, not scope). `templates/spec/design.md`'s `## Review Results` HTML comment mentions `Reviewed through: <sha>` as a separate line below `Reviewer mode` / `Verdict`. A new conformance test `risk_defines_shared_bounded_fix_judgment_and_reviewed_through` asserts both files' new wording, including the own-line placement and the code-only qualifier. `mochiflow freeze` is run and the regenerated `engine/MANIFEST.json` is committed with this task so `freeze --check` passes.
  - Stop: if stating the shared judgment would require a new `spec.yaml` field, enum, or lint rule (rather than prose the other three files can reference), stop and reconcile in design — the judgment stays an agent call, not a schema addition.
- [ ] T-002 [AC-02, AC-04, AC-05, AC-09, AC-11] update.md + router.md: hold vs. explicit finalize split
  - Depends on: T-001
  - Files:
    - `engine/commands/update.md`
    - `engine/router.md`
    - `engine/MANIFEST.json`
    - `cli/crates/mochiflow-cli/tests/conformance.rs`
  - Done: `update.md` treats its existing bare natural-language triggers (`修正依頼`, `PR feedback`, `PRを直して`, and equivalent hold-only requests with no slug qualifier) as hold-only: apply the requested change, commit locally with build discipline, re-verify, update the touched AC Matrix rows, then stop and report a status line noting the change is held (not pushed or reviewed) — no automatic push, `mochiflow accept`, or reviewer run. A distinct explicit finalize signal — the explicit `mochiflow-update` command, a match against any of `update.md`'s own `{slug} <pattern>` trigger patterns (`{slug} update`, `{slug} feedback`, `{slug} 修正依頼`, `{slug} PR feedback`), or an unambiguous "that's everything / push it / update the PR" statement — runs the existing review-if-stale-once / push / PR-metadata steps over every commit held since the last recorded `Reviewed through` sha (not just the latest one); when no **code-changing** commit exists beyond that sha (e.g. a PR-body-only correction), the reviewer does not re-run. `update.md`'s own frontmatter `description` ("update re-verifies, pushes, updates PR metadata...") is reworded to the same hold-by-default / finalize-pushes contract so it does not contradict the procedure it summarizes, and its Presentation-section line "Report what changed, what was re-verified, and what was pushed" is made conditional on finalize actually having run. A new stop condition states: do not push automatically on a hold-only message, and do not guess finalize from an ambiguous reply — ask one clarifying question instead. Every phrase pinned by the existing `pr_feedback_routes_to_update_without_restore` test remains verbatim: "do not move it", "do not revert", "bounded inline PR-feedback fix", "Build's eligibility gate", "flat". `engine/router.md`'s two descriptive spots for `update` — the "PR Feedback Loop Routing" sentence and the Verb Delegation table row — are both reworded so neither asserts an unconditional immediate push (matching the hold-by-default contract), while the routing decision itself and the pinned substring "applies bounded inline fixes" stay unchanged; no other router.md line or trigger/routing decision logic is touched. New conformance tests `update_holds_fixes_until_explicit_finalize_signal` and `router_update_summary_matches_hold_by_default` assert the above; the existing `pr_feedback_routes_to_update_without_restore` test still passes unmodified. `mochiflow freeze` is run and the regenerated `engine/MANIFEST.json` is committed with this task.
  - Stop: if separating hold from finalize would require changing `router.md`'s trigger/routing decision logic (beyond the one in-scope wording fix to the PR-feedback-routing sentence), stop and reconcile in design.
- [ ] T-003 [AC-06] open.md: generalize the freshness trigger at the accept gate
  - Depends on: T-001
  - Files:
    - `engine/commands/open.md`
    - `engine/MANIFEST.json`
    - `cli/crates/mochiflow-cli/tests/conformance.rs`
  - Done: `open.md` step 6 (immediately before `mochiflow accept {slug}` runs) gains an unconditional precondition, for `risk >= elevated`: when any **code-changing** commit exists beyond the recorded `Reviewed through` sha — whether it came from 3e's QA-`FAIL` rework or from a held post-build bounded fix carried into `open` — re-run `change-reviewer` once on the full diff, record the fresh verdict and updated `Reviewed through: <sha>`, then proceed to `mochiflow accept`. This check fires **independent of whether the QA round-trip (step 3) ran at all**, so an elevated spec with only automated QA (which skips step 3 entirely per step (a).2) is still covered — this is the fix for the gap where placing the trigger only inside 3e would silently skip freshness enforcement on that path. Step 3e's own wording is updated to reference the step-6 gate as the single place the re-review happens, rather than restating a second, independent re-review instruction; its existing round-based batching (multiple failed items in one round share one bounded fix) is unchanged. The optional `docs(context)` commit from step (c) is explicitly noted as not triggering the step-6 check by itself (not a code-changing commit). The existing round-trip step order (3a-3f), the two delivery gates, and the step markers pinned by `open_orders_acceptance_fold_accept_pr_gate` ("### (a) Acceptance" through "### (g) Push and create the PR") remain unchanged. A new conformance test `open_generalizes_freshness_trigger_to_reviewed_through` asserts the step-6 accept-gate wording and that it is reachable independent of the QA round-trip; `open_orders_acceptance_fold_accept_pr_gate` still passes unmodified. `mochiflow freeze` is run and the regenerated `engine/MANIFEST.json` is committed with this task.
  - Stop: if generalizing the trigger would change the QA round-trip protocol's step order (3a-3f) or the two delivery gates, stop — that is out of scope.
- [ ] T-004 [AC-02, AC-07] build.md: document the post-completion bounded-fix window
  - Depends on: T-001
  - Files:
    - `engine/commands/build.md`
    - `engine/MANIFEST.json`
    - `cli/crates/mochiflow-cli/tests/conformance.rs`
  - Done: `build.md` documents that after all tasks (or the single logical-unit commit for taskless/micro specs) complete and before `open` runs, an in-scope request is applied and committed locally with no `Task:` trailer (mirroring the existing convention already used for `open`'s QA-`FAIL` rework and `update`'s PR-feedback fix commits) and held — no reviewer run, no reopening of the task loop. An out-of-scope request keeps routing to `plan`, and the existing stop-condition sentence ("Stop when an out-of-scope change or a new design decision is needed.") remains present verbatim. The existing `prevent-build-phase-spec-mutation` task-structure rule (step 3d) is untouched. A new conformance test `build_documents_post_completion_bounded_fix_window` asserts the new section's wording and that the out-of-scope stop-condition sentence is still present; `build_ends_at_approved_without_pr_or_move` still passes unmodified. `mochiflow freeze` is run and the regenerated `engine/MANIFEST.json` is committed with this task.
  - Stop: if the post-completion window would require reopening `tasks.md`'s checkbox loop or ticking a task for the held fix, stop — this window is explicitly outside the task loop (no `Task:` trailer, no checkbox).
- [ ] T-005 [AC-08, AC-10] Final dogfood sync, full regression check, and default verification
  - Depends on: T-001, T-002, T-003, T-004
  - Files:
    - `engine/MANIFEST.json`
    - `cli/`
  - Done: with T-001..T-004 already having frozen the manifest, run the remaining dogfood (`mochiflow freeze` idempotently to confirm `engine/MANIFEST.json` is current; `mochiflow upgrade --source engine` re-vendors `.mochiflow/engine/`; `mochiflow adapter generate --check` passes with no drift); the surface `default` profile, `mochiflow lint --spec review-cadence-batching`, and `mochiflow doctor` (the project's quality gate per `AGENTS.md`, covering specs/config/ADR-structural-integrity/adapters/engine-integrity/index-drift) all pass. The full `cargo test` suite passes, explicitly including the pre-existing `pr_feedback_routes_to_update_without_restore`, `build_ends_at_approved_without_pr_or_move`, `open_orders_acceptance_fold_accept_pr_gate`, `engine_open_update_close_defined_no_ship_verb`, and `branch_placeholders_use_prefix_slug` (regression guard for AC-08: no change to the two delivery gates, the task-structure mutation rule, or the reviewer contract), alongside the five new tests from T-001..T-002 and T-003..T-004. AC Matrix automated rows recorded.
  - Stop: if a conformance fixture failure reveals an unintended contract change (not just the planned wording), stop and reconcile before forcing the fixture.
