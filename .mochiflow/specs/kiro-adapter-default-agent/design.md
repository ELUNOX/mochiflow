# Kiro adapter: drop dedicated agent, single always-on steering, delegate permissions — Design

## Design Decisions

- **Two-file generated surface.** Replace the dedicated `spec-builder.json` and
  the eight `spec*.md` verb steering files with one always-on steering file
  (`.kiro/steering/mochiflow.md`) plus the unchanged read-only reviewer agent
  (`.kiro/agents/spec-independent-reviewer.json`). Rationale: parity with the
  `AGENTS.md` / `CLAUDE.md` prose-enforcement model, minimal generated surface,
  and minimal `engine/MANIFEST.json` churn. Rejected: keeping a minimal agent
  only to wire reviewer-subagent trust — the host default agent can invoke the
  reviewer with an accepted per-call allow.
- **`#[[file:...]]` pointers, not inlined context.** `mochiflow.md` includes the
  router, both constitution files, and the three context files by Kiro-native
  reference. Rejected inlining: it would re-render `mochiflow.md` on every
  `refresh-context` and bloat engine integrity hashing, buying no real
  certainty over the pointer mechanism already used for the router. Primary
  source: Kiro steering `#[[file:...]]` file-reference semantics
  (Kiro docs, current `.kiro/steering` behavior).
- **No baked `toolsSettings`.** All shell/write/web_fetch policy is removed from
  generated Kiro JSON; guardrails live in `mochiflow.md` Rules prose. Permissions
  are owned by the user's `permissions.yaml` (per-user, outside the repo, so not
  vendorable). The `git push` / provider-PR invariant becomes prose, matching
  Claude/AGENTS.
- **Self-healing regeneration (alpha, breaking-OK).** Rather than a migration
  command, `adapter generate` carries a static list of deprecated Kiro output
  paths and removes those that contain the `generated by mochiflow` marker,
  reporting each. Marker-less (user-edited / hand-authored) files are skipped.
  Rationale: the marker is already the system's reliable "mochiflow-managed"
  signal (used by `detach`); this self-heals existing projects (including this
  dogfood repo) without risking user files such as `.kiro/steering/release.md`.
  Known limitation (out of scope): `detach`'s `plan_adapter_cleanup` iterates
  only the current manifest `[files]`, so after the manifest shrinks, `detach`
  on a never-regenerated project no longer cleans the old `spec-builder.json` /
  `spec*.md`. Acceptable for alpha; left as a documented limitation.
- **No `VERSION` / `contracts.lock` bump.** The changed contract is the
  `engine/`-resident adapter output (hashed into `engine/MANIFEST.json`), not a
  `contracts/*.json` schema. The dogfood sequence (`mochiflow freeze` →
  `mochiflow upgrade --source engine` → `mochiflow adapter generate`) restores
  integrity; `doctor` / `freeze --check` confirm. A `VERSION` bump is performed
  only if `doctor`/`freeze` reveal a schema-surface change (not expected).

## Architecture

- `engine/adapters/kiro/manifest.toml` — new `[files]` map: only
  `.kiro/steering/mochiflow.md` → `steering/mochiflow.md.tpl` and
  `.kiro/agents/spec-independent-reviewer.json` → its template. Remove the
  `spec-builder.json` and all `spec-*.md` entries.
- `engine/adapters/kiro/steering/mochiflow.md.tpl` — new template:
  `inclusion: always` frontmatter, marker comment, `#[[file:...]]` includes for
  `{{engine}}/router.md`, `{{constitution.project}}`, `{{constitution.local}}`,
  `{{context.product}}`, `{{context.structure}}`, `{{context.tech}}`, and a Rules
  block mirroring `AGENTS.md.tpl` (push/PR via `mochiflow pr`, patch↔plan
  escalation, spec lifecycle, verify via surfaces, lint/doctor, languages).
- **`mochiflow.md` must be full-file managed, not block-appended.** A Kiro
  steering file requires `inclusion: always` frontmatter at the very top of the
  file. The generic `.md` embeddable path appends a managed block to a markerless
  pre-existing file, which would place the frontmatter mid-file and break the
  always-on guarantee while `adapter generate --check` still passed. Treat
  `.kiro/steering/mochiflow.md` (and any Kiro steering `.md`) as a full-file
  managed target: a fresh or marker-bearing file is written whole (frontmatter at
  top); a markerless pre-existing file is overwritten whole (not appended). This
  matches how the freshly-written / marker-present cases already behave; only the
  markerless-pre-existing case changes from append to overwrite.
- Delete `engine/adapters/kiro/agents/spec-builder.json.tpl` and the seven
  `engine/adapters/kiro/steering/spec-*.md.tpl` plus `spec.md.tpl`.
- `cli/crates/mochiflow-core/src/adapter.rs` — update `is_kiro_agent_json` to
  only `.kiro/agents/spec-independent-reviewer.json`; add the deprecated-path
  self-heal in `generate` (and surface removals in `AdapterResult`); keep
  `preserve_kiro_agent_model` / `kiro_agent_matches_rendered` working for the
  reviewer agent.
- `cli/crates/mochiflow-core/src/doctor.rs` — `doctor` reuses `generate(check)`
  and maps every drift entry to FAIL, so there is one rule, not two: `doctor`
  (and `adapter generate --check`) FAIL while un-healed marker-bearing deprecated
  residue is present, and clear once `mochiflow adapter generate` (write mode)
  removes it. Both detection (check branch) and removal (write branch) are driven
  by a single shared deprecated-path list so they cannot disagree.

## Data Model / Interfaces

- `AdapterResult` gains a way to report removed deprecated files (e.g. a
  `removed: Vec<String>`) and preserved/skipped marker-less deprecated files
  (e.g. `preserved: Vec<String>`), printed by `cmd_adapter_generate` alongside
  `wrote:` as `removed:` / `preserved:` lines. Detection and removal are driven
  by **one** shared static deprecated-path list, scanned in **both** the
  write branch (remove markered, record preserved) and the check branch (report
  lingering markered residue as drift so `--check` / `doctor` catch
  un-regenerated projects).
- Deprecated Kiro path list (static, kiro-only):
  `.kiro/agents/spec-builder.json`, `.kiro/steering/spec.md`,
  `.kiro/steering/spec-discuss.md`, `-plan`, `-build`, `-ship`, `-patch`,
  `-review`, `-refresh-context`, and `.kiro/hooks/generate-project-index.kiro.hook`
  (a 1.1.0-era managed hook the current adapter no longer generates — confirmed
  by the `cli.rs` test `kiro_adapter_ignores_existing_custom_hooks`; it is
  marker-bearing and tracked in this dogfood repo, so it must be on the list or
  the two-file contract is violated after regeneration).
- The scan only ever inspects paths on this static list; files not on it (e.g. a
  user's `.kiro/steering/release.md`) are never read, never removed, and never
  reported.
- Marker check reuses `MARKER_PREFIX` (`generated by mochiflow`).

## Error Handling

- Removal that hits `NotFound` is a no-op success (file already gone).
- A deprecated path **on the static list** present but lacking the marker is
  skipped, recorded in `preserved`, and reported (never deleted).
- A file **not on the static list** (any other `.kiro/` content, e.g.
  `release.md`) is never scanned: untouched and unreported.
- Self-heal applies only to the `kiro` adapter; other adapters are unaffected.

## Test Strategy

- Update `conformance.rs` `materialize_full` assertions: expect
  `.kiro/steering/mochiflow.md` (not `spec.md`) and
  `.kiro/agents/spec-independent-reviewer.json`; drop `spec-builder.json`
  existence. Replace the blocked-merge case (`spec-builder.json`) with the
  remaining JSON target `.kiro/agents/spec-independent-reviewer.json` — never
  with `mochiflow.md`, which is an embeddable `.md` target that always
  merges/appends a managed block and never reaches the BLOCKED-candidate branch,
  so it cannot exercise blocked-merge coverage.
- Update `cli.rs` cases that write/inspect `spec-builder.json`.
- Update `present.rs` doctests/tests referencing `spec-builder.json` messages.
- New unit test in `adapter.rs`: deprecated markered files removed + reported;
  a markerless deprecated-list file preserved + reported; a non-listed file
  (`release.md`) untouched + unreported; reviewer agent still generated.
- New test for full-file-managed steering: a markerless pre-existing
  `.kiro/steering/mochiflow.md` is overwritten whole (frontmatter at top), not
  block-appended.
- Dogfood integrity: after edits run `mochiflow freeze`,
  `mochiflow upgrade --source engine`, `mochiflow adapter generate`, then
  `mochiflow doctor` and `cargo test`. Confirm this repo's tracked
  `.kiro/hooks/generate-project-index.kiro.hook` is removed and `release.md`
  remains.

## Integration Contract

- Contract owner: the `kiro` adapter generator (mochiflow), consumed by Kiro CLI.
- Output contract (new): `.kiro/steering/mochiflow.md` (`inclusion: always`,
  `#[[file:]]` includes + Rules) and `.kiro/agents/spec-independent-reviewer.json`
  (read-only). No other mochiflow-managed files under `.kiro/` — including the
  legacy `.kiro/hooks/generate-project-index.kiro.hook`, which self-heal removes.
- Compatibility: breaking for the prior Kiro layout (alpha, accepted). Existing
  projects are converged by self-healing regeneration, not a migration command.
- Permissions: delegated entirely to the user's `permissions.yaml`; mochiflow
  emits nothing for it. Guardrails are advisory prose in `mochiflow.md`.
- Failure handling: if generation cannot write the new files, existing behavior
  (error reporting in `AdapterResult.errors`) applies; deprecated files are only
  removed after their marker is confirmed.
- Verification: `mochiflow adapter generate --check` + `mochiflow doctor` +
  `cargo test`.

## Review Results

<!-- Recorded during build: independent-reviewer once after all tasks (risk: elevated). Reviewer mode: delegated | inline / Verdict: pass | pass-with-comments | fail -->
