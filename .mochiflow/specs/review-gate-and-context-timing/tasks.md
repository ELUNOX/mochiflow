# Pre-approval review gate and PR-shipped context refresh — Tasks

Implementation Summary: Move review before the approve-to-build gate for risk>=elevated, and ship open-triggered context refresh inside the PR; then re-freeze the engine.
risk: elevated
Critical Stop Conditions:
- Do not make review mandatory or add a third delivery gate (keep exactly two: approve-to-build, approve-PR).
- Do not widen `mochiflow accept` staging to include `[context]` files; context ships via a separate `docs(context)` commit.
- Do not commit on verification FAIL; engine edits require a re-freeze so `freeze --check` passes.

## Defaults

- Per-task verification: run `mochiflow freeze` first (engine edits change the markdown hashes, so the manifest must be regenerated for `freeze --check` to pass), then `cargo test --manifest-path cli/Cargo.toml && cargo fmt --manifest-path cli/Cargo.toml --all -- --check && cargo clippy --manifest-path cli/Cargo.toml --all-targets -- -D warnings && cargo run --manifest-path cli/Cargo.toml -- freeze --check`. Each engine-editing task therefore includes the freshly frozen `engine/MANIFEST.json` in its own commit so every commit is self-consistent.
- The remaining dogfood steps (`mochiflow upgrade --source engine` to re-vendor `.mochiflow/engine/`, `mochiflow adapter generate --check`) are the final sync in T-003; the vendored copy is gitignored and adapters are byte-identical for prose-only edits, so they are not needed per task.
- Engine edits target repo-root `engine/...` only; never edit the gitignored vendored `.mochiflow/engine/` copy (it is regenerated by `upgrade --source engine`).
- Shared stop conditions: out-of-scope change / new design decision needed / verification keeps failing

## Tasks

- [x] T-001 [AC-01, AC-02, AC-03, AC-04, AC-10, AC-11] Change A: pre-approval review for risk>=elevated + reviewer plan-quality mode + non-gate clarification
  - Depends on: none
  - Files:
    - `engine/commands/plan.md`
    - `engine/agents/independent-reviewer.md`
    - `engine/commands/review.md`
    - `engine/reference/risk.md`
    - `engine/reference/workflow.md`
    - `engine/MANIFEST.json`
    - `cli/crates/mochiflow-cli/tests/conformance.rs`
  - Done: For `risk >= elevated`, plan.md step 7 readiness card offers Review (recommended) before the confirm-plan action; pre-approval review runs `mochiflow-review` on the draft spec in the reviewer's plan-quality mode and on `fail` reports findings while leaving `status: draft` (no plan commit); steps 8-10 and stop conditions aligned so review is consumed pre-approval and not re-offered post-approval for elevated; `risk = standard` flow unchanged. `independent-reviewer.md` defines a plan-quality mode (Stage 1 spec/design/tasks conformance + spec-artifact quality, no diff/changed-files input) and widens `phases`; `review.md` notes ad-hoc review on a code-less spec uses this mode; `risk.md ## Review transport`/`## Ad-hoc review` notes a code-less spec uses the no-diff mode. `workflow.md` states review is a quality assist, not a delivery gate (two gates retained). A conformance test asserts the elevated-before-confirm ordering and the unchanged standard order. `mochiflow freeze` is run and the regenerated `engine/MANIFEST.json` is committed with this task so `freeze --check` passes. All edited files internally consistent (no dangling references to the old ordering).
  - Stop: if a plan-quality reviewer mode would require splitting a second reviewer agent or changing the build-time risk-cadence review contract, stop and reconcile in design.
- [ ] T-002 [AC-05, AC-06, AC-07, AC-08] Change B: ship open-triggered context refresh in the PR + pin commit order + re-pin conformance test
  - Depends on: none
  - Files:
    - `engine/commands/open.md`
    - `engine/commands/refresh-context.md`
    - `engine/reference/git.md`
    - `engine/router.md`
    - `engine/MANIFEST.json`
    - `cli/crates/mochiflow-cli/tests/conformance.rs`
  - Done: open.md step 4 runs `refresh-context` on the feature branch under human confirmation; `open` owns the `git add` of context paths and a separate `docs(context)` commit (with `Spec:` trailer) positioned **after** the fold/context-check and **before** the `mochiflow accept` close-out commit, with open's `(a)-(f)` sequence updated to show this; the accept close-out remains the single final state commit. refresh-context.md "When it runs", procedure, **and frontmatter `description`** describe the in-branch-before-PR primary path while keeping its no-auto-commit contract (commit is open's responsibility). git.md `## Living-spec fold` + `## Auto-commit and staging` document the in-PR primary path, the preceding `docs(context)` commit, and demote post-merge refresh to the at/after-merge fallback (follow-up `fix` spec or backlog seed). router.md's `open` summary mentions the optional `docs(context)` commit before accept. No document (body or frontmatter) presents post-merge refresh as the primary path for open-detected staleness; the `open_defers_context_refresh_until_after_pr_or_merge` conformance test is rewritten (rename allowed) to assert the NEW in-PR-primary contract (not loosened/deleted). `mochiflow freeze` is run and the regenerated `engine/MANIFEST.json` is committed with this task so `freeze --check` passes. All edited files cross-reference consistently.
  - Stop: if shipping context in-PR would require changing `mochiflow accept`/`mochiflow pr` staging or pre-flight contracts, stop — that is out of scope and signals a design gap.
- [ ] T-003 [AC-09] Final dogfood sync and full default verification
  - Depends on: T-001, T-002
  - Files:
    - `engine/MANIFEST.json`
    - `cli/`
  - Done: with T-001/T-002 already having frozen the manifest, run the remaining dogfood (`mochiflow freeze` idempotently to confirm `engine/MANIFEST.json` is current; `mochiflow upgrade --source engine` re-vendors `.mochiflow/engine/`; `mochiflow adapter generate --check` passes); the surface `default` profile and `mochiflow lint --spec review-gate-and-context-timing` both pass; both conformance tests (Change A ordering + rewritten Change B) pass. AC Matrix automated rows recorded.
  - Stop: if a conformance fixture failure reveals an unintended contract change (not just the planned wording), stop and reconcile before forcing the fixture.
